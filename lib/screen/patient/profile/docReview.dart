import 'package:flutter/material.dart';

class DoctorReview extends StatelessWidget {
  const DoctorReview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
